- Print pipeline diagnostic: CUPS/Brother queue is healthy for PDF; set user default destination/options to `Brother_DCP_L2540DW_series` with Letter/grayscale/simplex. Root cause for DOCX failure is direct DOCX submission to CUPS plus missing `libreoffice-writer`. Added `scripts/print-standard-doc.sh` and `data/print-pipeline-diagnostic-2026-05-18.md`; PDF test job `Brother_DCP_L2540DW_series-16` was accepted successfully. Next command for full DOCX support: `sudo apt-get update && sudo apt-get install -y libreoffice-writer`.
- Print pipeline diagnostic: CUPS/Brother queue is healthy for PDF; set user default destination/options to `Brother_DCP_L2540DW_series` with Letter/grayscale/simplex. Root cause for DOCX failure is direct DOCX submission to CUPS plus missing `libreoffice-writer`. Added `scripts/print-standard-doc.sh` and `data/print-pipeline-diagnostic-2026-05-18.md`; PDF test job `Brother_DCP_L2540DW_series-16` was accepted successfully. Next command for full DOCX support: `sudo apt-get update && sudo apt-get install -y libreoffice-writer`.
- Amanda Claire resume work: tailored resume for Full Circle Birmingham In-Store Program Facilitator using local job-search/Indeed assets where possible; direct Indeed fetch hit 403. Corrected assumptions: no Confetti Closet content, retail experience attributed to Storkland. Outputs created under `outputs/amanda-resumes/`: tailored TXT, DOCX, summary MD, and styled HTML/PDF. Verified tailored resume includes Storkland and excludes Confetti. Uploaded DOCX to Google Drive: `https://docs.google.com/document/d/1mtvsvqpgdA42-AxfiCDzf-Hc_q0J4NXs/edit?usp=drivesdk&ouid=107739831402166725443&rtpof=true&sd=true`. Commits: `eed6ab9 Tailor Amanda resume for Full Circle role`, `433eae1 Add Brother print pipeline wrapper`.
- Printing: initial direct DOCX print failed with `lp: Unsupported document-format "application/vnd.openxmlformats-officedocument.wordprocessingml.document"`. Printed text fallback job `Brother_DCP_L2540DW_series-15`; PDF path verified with `Brother_DCP_L2540DW_series-16`; styled PDF reportedly printed as `Brother_DCP_L2540DW_series-17`.
- Model/runtime correction: Chris noticed status showed GPT 5.4 instead of 5.5. Live session override was corrected so status showed `openai-codex/gpt-5.5`. Chris asked whether to switch main to Codex harness; recommendation was to keep main on OpenClaw Pi and use Codex for coding/subagents. Chris chose: “Stick with pi.”
- Incident correction: A prior OpenClaw incident message claimed provider auth degraded (`minimax-portal` OAuth expired and `openai-codex` refresh failing with `refresh_token_reused`). Chris said it was inaccurate. Live check found gateway/channels and `openai-codex/gpt-5.5` auth working; only degradation seen was event-loop/CPU utilization. Treat that prior SEV-3 as invalid unless real auth failure appears in logs.
- LibreOffice update: Chris later reported “Libreoffice installed,” so DOCX conversion/printing support should now be available through the standard print wrapper, pending verification with a real DOCX print test.
- Codex command/function follow-up: User asked whether Codex accepts `/goal`; initial answer incorrectly checked only OpenClaw slash commands and said no. User corrected: “Codex has a goal function. Look it up.” Next assistant should inspect Codex app-server/harness tool/function surface (not only `/codex` slash command docs) for a `goal` function and explain accurately whether it is callable as `/goal`, a Codex tool/function, or via `/codex steer`/thread instructions.
- Codex `/goal` follow-up: user asked if we can update to get the feature. Verified global Codex was `0.115.0`; latest stable identified as `0.130.0`. In temp/available release check, `0.130.0` includes `goals experimental false`, and `codex features enable goals` works. Important caveat: OpenClaw Codex harness uses its own managed Codex app-server binary by default, so updating global `@openai/codex` likely enables `/goal` in terminal Codex but may not expose it through Telegram/OpenClaw unless the harness/plugin managed binary or command override also supports it. Recommended update path shared with user: `npm install -g @openai/codex@0.130.0`, `codex features enable goals`, verify with `codex -V` and `codex features list | grep goals`; rollback with `npm install -g @openai/codex@0.115.0`. Await explicit approval before actually updating.
- Codex CLI update approved and completed: global `@openai/codex` updated from `0.115.0` to `0.130.0`; `codex features enable goals` succeeded; verification shows `goals experimental true` and `npm ls -g @openai/codex --depth=0` reports `@openai/codex@0.130.0`. Note: commands still print a non-fatal warning `failed to clean up stale arg0 temp dirs: Permission denied`; not caused by this update. OpenClaw itself and its managed Codex harness were not updated.
