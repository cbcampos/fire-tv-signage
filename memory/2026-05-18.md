- Print pipeline diagnostic: CUPS/Brother queue is healthy for PDF; set user default destination/options to `Brother_DCP_L2540DW_series` with Letter/grayscale/simplex. Root cause for DOCX failure is direct DOCX submission to CUPS plus missing `libreoffice-writer`. Added `scripts/print-standard-doc.sh` and `data/print-pipeline-diagnostic-2026-05-18.md`; PDF test job `Brother_DCP_L2540DW_series-16` was accepted successfully. Next command for full DOCX support: `sudo apt-get update && sudo apt-get install -y libreoffice-writer`.
